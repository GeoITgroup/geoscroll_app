name: geoscroll_app
description: "GeoScroll - Scooter Rental App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  mobile_scanner: ^5.2.3
  url_launcher: ^6.3.2
  cupertino_icons: ^1.0.6
  geolocator: ^11.0.0
  google_sign_in: ^6.2.1
  google_maps_flutter: ^2.17.1
  firebase_core: ^4.9.0
  firebase_messaging: ^16.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true